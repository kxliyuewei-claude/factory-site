# Cloudflare Pages Fallback

Cloudflare Pages is not configured in this run.

If GitHub Pages is unavailable, deploy this directory with Wrangler after logging in:

```sh
wrangler pages deploy /Users/tlf/Desktop/workspace/money/ios-apps/factory-site --project-name factory-site
```
